var patrons = []
for(i = 0; i < 100; i++){
	var age = Math.floor(Math.random() * (97 - 18) ) + 18;
	var person = {'_id': i.toString(), 'x':, 'y':, 'state':{'age': age, 'drinkLevel': 0, }}
	patrons.push()
}